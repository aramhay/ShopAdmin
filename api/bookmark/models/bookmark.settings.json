{
  "kind": "collectionType",
  "collectionName": "bookmarks",
  "info": {
    "name": "bookmark"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "users_permissions_user": {
      "plugin": "users-permissions",
      "model": "user",
      "via": "bookmarks"
    },
    "video": {
      "model": "videos",
      "via": "bookmarks"
    }
  }
}
